# Latest versions are available on https://pypi.org
#
# For updating to latest versions you should run:
#
#   $ pushd ~/git/truedoc && pur
#

alembic == 1.2.1        # Direct use: https://pypi.org/project/alembic/
blinker == 1.4          # Dependence: for 'sentry-sdk'
celery == 4.3.0         # Direct use: https://pypi.org/project/celery/
Flask == 1.1.1          # Direct use: https://pypi.org/project/Flask/
marshmallow == 3.2.1    # Direct use: https://pypi.org/project/marshmallow/
pur == 5.2.2            # Dev: updates requirements.txt to the latest versions — https://pypi.org/project/pur/
PyJWT == 1.7.1          # Direct use: https://pypi.org/project/PyJWT/
PyMySQL == 0.9.3        # Direct use: https://pypi.org/project/PyMySQL/
requests == 2.22.0      # Dependence: for directory 'api-requests': https://pypi.org/project/requests/  TODO: Move the directory to tests
sentry-sdk == 0.12.3    # Direct use: https://pypi.org/project/sentry-sdk/
SQLAlchemy == 1.3.9     # Direct use: https://pypi.org/project/SQLAlchemy/
